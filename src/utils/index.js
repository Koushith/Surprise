export * from './colors';
export * from './typography';
export * from './themes';
export * from './Global';

/**
 * if we didn't have this index file, while using this we need to use something like
 * import {primaryFront} from '../utils/typography'
 *
 * it's lot nicer if we just inclue files from utils
 *
 * eg: import {primaryFront, xxx,xxx} from '../utils
 */
